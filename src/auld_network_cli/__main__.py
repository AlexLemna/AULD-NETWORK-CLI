"""Entry point for running the Auld Network CLI as a module.

This allows the package to be run with:
    python -m auld_network_cli
"""

from __future__ import annotations

from auld_network_cli import main

if __name__ == "__main__":
    main()
